# 支付服务环境变量示例
# 复制为 .env 后修改

NODE_ENV=development
PAYMENT_PORT=3001
LOG_LEVEL=info

# MongoDB
MONGODB_URI=mongodb://localhost:27017/payment_db
# 或
# PAYMENT_MONGODB_URI=mongodb://localhost:27017/payment_db

# 测试专用（Day 6：避免 mongodb-memory-server 首次下载导致超时）
# 推荐：在 CI/本地显式提供测试库连接
# TEST_MONGODB_URI=mongodb://127.0.0.1:27017/payment_test
# 可选：本机 Mongo 兜底地址（未配置 TEST_MONGODB_URI 时尝试）
# TEST_MONGODB_FALLBACK_URI=mongodb://127.0.0.1:27017/payment_test
# 可选：最后兜底启用 mongodb-memory-server（可能触发首次下载，不建议 CI 默认使用）
# USE_MONGODB_MEMORY_SERVER=false
# 可选：固定 mongodb-memory-server 二进制版本（用于缓存命中）
# MONGOMS_VERSION=7.0.8

# 可选
PAYMENT_DEFAULT_CURRENCY=CNY
PAYMENT_TIMEOUT_MINUTES=30

# 外部依赖（ADR-005 / ADR-006）
# CORE_BANK_SERVICE_URL=http://localhost:8080
# REDIS_URI=redis://127.0.0.1:6379
