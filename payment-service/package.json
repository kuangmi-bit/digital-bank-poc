{
  "name": "payment-service",
  "version": "1.0.0",
  "description": "数字银行 POC - 支付清算处理器服务",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest --runInBand",
    "test:coverage": "jest --runInBand --coverage --coverageDirectory ../tests/reports/coverage/payment-service",
    "test:unit": "jest --testPathPattern=payment-service.test",
    "test:api": "jest --testPathPattern=api.test --runInBand"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [
    "payment",
    "settlement",
    "digital-bank",
    "express",
    "mongodb"
  ],
  "author": "Digital Bank POC Team",
  "license": "MIT",
  "dependencies": {
    "bull": "^4.12.0",
    "express": "^4.21.0",
    "ioredis": "^5.4.1",
    "mongoose": "^8.8.0",
    "winston": "^3.14.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "mongodb-memory-server": "^10.0.0",
    "supertest": "^7.0.0"
  }
}
