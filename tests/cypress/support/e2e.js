/**
 * Cypress E2E 支持：全局钩子、自定义命令
 * 遵循 naming-conventions：describe/it 描述性命名
 */

// 导入命令（若后续添加）
// import './commands';

// 全局 before/beforeEach/afterEach
beforeEach(() => {
  // 可设置 cookie、localStorage、拦截等
});

afterEach(() => {
  // 可选清理
});
