# 测试用例池

YAML 格式，符合 `test-case-schema.json`。id 格式 `TC-{suite}-{nnn}`。

- `core-bank-api.yaml`
- `payment-api.yaml`
- `risk-api.yaml`
- `e2e-smoke.yaml`

可转为 CSV 列：id, title, suite, priority, steps, expected, preconditions, tags。
