apiVersion: v1
kind: Service
metadata:
  name: risk-service
  labels:
    app: risk-service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: risk-service
