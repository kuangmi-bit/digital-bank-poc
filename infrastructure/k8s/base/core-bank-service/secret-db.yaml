# 核心银行服务 - 数据库连接 Secret (Day 3 Agent 5)
# 仅用于 POC/Dev；生产环境须使用 Vault 或外部密钥管理，勿将真实密码提交代码库
# 依赖: Postgres Service (postgres.digital-bank-poc.svc.cluster.local) 需由 Agent 9 部署

apiVersion: v1
kind: Secret
metadata:
  name: core-bank-db-secret
  labels:
    app: core-bank-service
type: Opaque
stringData:
  # 与 application.yml 默认 CORE_BANK_DB_PASSWORD 一致，供 Dev 使用
  password: "digitalbank"
